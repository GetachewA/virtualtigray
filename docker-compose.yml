services:
  postgres:
    image: postgres:15-alpine
    restart: always
    environment:
      POSTGRES_DB: virtualtigray
      POSTGRES_USER: vtg_user
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./database/init.sql:/docker-entrypoint-initdb.d/init.sql:ro
    networks:
      - virtualtigray_network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -h 127.0.0.1 -U vtg_user -d virtualtigray"]
      interval: 5s
      timeout: 5s
      retries: 20
      start_period: 30s

  backend:
    image: node:18-alpine
    restart: always
    working_dir: /app
    volumes:
      - ./backend:/app
    command: sh -c "npm install --omit=dev && npm start"
    environment:
      NODE_ENV: production
      PORT: 3000
      DB_HOST: postgres
      DB_PORT: 5432
      DB_NAME: virtualtigray
      DB_USER: vtg_user
      DB_PASSWORD: ${DB_PASSWORD}
      JWT_SECRET: ${JWT_SECRET}
      CORS_ORIGIN: ${CORS_ORIGIN:-*}
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - virtualtigray_network
    labels:
      - traefik.enable=true
      - traefik.http.routers.virtualtigray.rule=Host(`virtualtigray-api.apps.apparium.ca`)
      - traefik.http.services.virtualtigray.loadbalancer.server.port=3000
      - coolify.managed=true

volumes:
  postgres_data:

networks:
  virtualtigray_network:
    driver: bridge
