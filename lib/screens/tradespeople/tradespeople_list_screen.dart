import 'package:flutter/material.dart';

class TradespeopleListScreen extends StatelessWidget {
  const TradespeopleListScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Local Tradespeople')),
      body: const Center(
        child: Text('Tradespeople Directory - Coming Soon'),
      ),
    );
  }
}
